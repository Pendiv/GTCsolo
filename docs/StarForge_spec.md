# StarForge 設計書 ver.0.4

> 継続更新: 仕様確定/変更があったらこのファイルを更新する。

---

## 1. 基本情報 (ver.0.1 から継承)

- **マシン名**: StarForge / SF / 恒星鍛造炉
- **外形**: 27³ の真球形マルチブロック (草案: `run/cmdex/export/StarForge.txt`)
- **コンセプト**: 星の一生をシミュレーションする機械
- **現行最強指標**:
  - 最大エネルギー消費 / 最大エネルギー生成
  - 最も加工時間の長いレシピを保有
  - 最大の出力アイテム種類数 / 出力数

---

## 2. レシピタイプ

- レシピタイプ: `gtcsolo:starforge` (1 つ)
- **スロット仕様**: アイテム入力 1 / アイテム出力 15 / 液体入力 0 / 液体出力 12
  - アイテム入力 1 = 軌跡 (star_locus) のみ。ただし「起動アイテム」「所定アイテム継続消費」は別ハッチ経由 (実態ロジック側で扱う)
- 入力: NBT 付き `gtcsolo:star_locus` (Trace で 8 軌跡識別、レシピ稼働で消費)
- 軌跡ごとにロジック固有 (= レシピ稼働中の挙動が分岐する、GT 標準のレシピデータには収まらない)
- 出力アイテム: **未確定** (後回し)

---

## 3. 軌跡分類

| 軌跡 | フェイズ構成 | 区分 | 特殊事項 |
|---|---|---|---|
| brown_dwarf  | 構築 → 崩壊        | 通常型     | — |
| koi74        | 構築 → 崩壊        | 通常型     | — |
| r_andromedae | 構築 → 崩壊        | 通常型     | — |
| hd101065     | 構築 → 崩壊        | 通常型     | — |
| cemp_r       | 構築 → 崩壊        | 通常型     | — |
| **sun**      | 構築 → **成熟** → 崩壊 | 成熟型 | ダイソンキューブ化 (継続発電) |
| neutron_star | 構築 → 崩壊        | 通常型     | — |
| **black_hole** | 構築 → **成熟** → 崩壊 | 成熟型 | 特殊: 崩壊度 + 超消費 + singularity 獲得チャンス |

---

## 4. 通常型レシピ (brown_dwarf / koi74 / r_andromedae / hd101065 / cemp_r / neutron_star)

### 4.1 構築フェイズ
- レシピは未稼働 (idle、**要求アイテム**待機状態)
- プレイヤーが要求アイテムを搬入 → **崩壊フェイズへ移行**

### 4.2 崩壊フェイズ
- レシピ稼働開始
- **所定のアイテム** (要求アイテムとは別物) を継続的に供給する必要
- 供給が止まると機械電源 OFF
- **累計消費量**が要求値に達するまで稼働
- 累計達成 → レシピ履行完了 → 出力アイテム搬出

---

## 5. 成熟型: 太陽 (sun)

### 5.1 構築フェイズ
- 通常型と同様: 要求アイテム搬入で**成熟フェイズ**へ

### 5.2 成熟フェイズ (ダイソンキューブ化)
- レシピに**加工時間 (duration)** が定められており、その間で動作
- フェイズ前半: 指定数の **GTCEu Solar Panel** を搬入するまで → エネルギー消費
- ソーラー搬入完了後 → 成熟中ずっと**エネルギー生成** (ダイソンキューブ稼働)
- エネルギー要求値を消費できなくてもレシピ稼働 (できる限り消費、不足分は持続時間に反映)
- duration 経過 → 崩壊フェイズへ

### 5.3 崩壊フェイズ
- ソーラーの一部返還
- 少量のアイテム

---

## 6. 成熟型: ブラックホール (black_hole) — 特殊

### 6.1 構築フェイズ
- 通常型と同様

### 6.2 成熟フェイズ
- 特殊: レシピ上はエネルギー消費しない
- 要求アイテム搬入 → 消費して**瞬間的に大量エネルギー放出**
- 完了 → 崩壊フェイズへ

### 6.3 崩壊フェイズ — 崩壊度 + 超消費

#### 崩壊度 (Decay %)
- UI 表示パラメータ
- 初期値: 100%
- 減衰: 毎秒 1% 低下
- 0% でフェイズ終了 (アイテム出力なし、ただし超消費中の電源 OFF で singularity 獲得可)

#### 内部閾値 (ロジック内、UI 非表示)
- ブラックホールレシピ稼働ごとに**ランダム生成**
- 値域: **0% 〜 92%**
- 崩壊度が「内部値 + 8%」に達したとき、**超消費開始**

例:
| 内部値 | 超消費開始時の崩壊度 | 超消費継続範囲 |
|---|---|---|
| 50% | 58% | 58% → 50% |
| 0%  | 8%  | 8% → 0% |
| 92% | 100% | 100% → 92% (開始直後) |

#### 超消費 (Surge Consumption)
- 持続: 8% 崩壊度減少分 = **160 tick (= 8 秒)**
- 消費電圧: MAX+14V × 1A = **2.30584e18 EU/t** (= 2^61)
- 消費の挙動 (StarForge 専用 EnergyHatch 経由、§7 参照):
  - 接続電線から**取れるだけエネルギー**を消費し続ける
  - WEN ネットワークからは tick あたり **2.14G EU (ハッチ容量分)** を消費
  - 合計 = 「電線吸い尽くし」+ 「WEN 容量分」
- 超消費中に**マシン電源を切る** (controller の active OFF) → **singularity 搬出 + 即座に崩壊フェイズ終了**

---

## 7. StarForge 専用 EnergyHatch

- 仮名: `StarForgeEnergyHatch`
- **WEN (Wireless Energy Network) 接続専用**
- 容量: **2,147,483,648 EU (= 2^31 = MAX 1A × 1tick 分)**
- 通常時: 容量内で受け取り、レシピへ供給
- 超消費時 (BH 崩壊フェイズの 160 tick):
  - 接続電線から最大限消費 (容量無視で吸い上げ続ける、特殊処理)
  - WEN ネットワークから tick あたり 容量分 (2.14G EU) 消費
- 既存 `gtcsolo:spaceforge_energy_hatch` 系の流儀を参考に新規実装

---

## 8. 設計書 ver.0.1 (前回) との整合性

- ver.0.1 §7「80tick」→ **160tick に訂正** (8% 崩壊度 = 160tick で確実性確保)
- それ以外整合

---

## 9. 設計判断 (実装方針)

- マルチブロック 1 つ + レシピタイプ 1 つ
- 軌跡ごとのロジック分岐: 通常型ベースクラス + 太陽サブクラス + ブラックホールサブクラス の 3 系
- 軌跡 8 種は recipe data に Trace を持たせて分岐
- StarForge 用 EnergyHatch は別マシン (新規実装、WEN 接続必須)

---

## 9.5 JEI 設計 (二層構造)

### 9.5.1 自作 JEI ページ (メイン情報源)
- 独自カテゴリ `gtcsolo:starforge_info` を実装 (GTCEu の `MultiblockInfoCategory` をお手本)
- 軌跡ごとに 1 ページ (合計 8 ページ)
- 表示内容:
  - 軌跡名 + 区分 (通常型 / 成熟型)
  - **初期要求アイテム** (構築フェイズ)
  - **総消費アイテム** (崩壊フェイズの累計、通常型のみ)
  - **フェイズ説明** (構築 → (成熟) → 崩壊 の流れ)
  - 太陽/ブラックホールは「特殊な稼働 (詳細は仕様書/ゲーム内挙動参照)」と注記
  - 出力アイテム (一覧)
- 実装層: `StarForgeInfoCategory` + `StarForgeInfoWrapper` + `StarForgeInfoWidget`

### 9.5.2 GT 標準レシピ JEI ページ (誘導用)
- レシピタイプ `gtcsolo:starforge` のレシピデータを 8 件登録 (ダミー)
- 表示内容:
  - 入力: Trace 付き star_locus
  - 出力: 各軌跡の出力アイテム (未確定だが入る)
  - EUt / duration:
    - 通常型: 「エネルギー消費なし」と表記 (実態は所定アイテム継続消費)
    - 太陽 / BH: 「特殊なレシピ稼働が行われる」と表記
  - **自作 JEI ページへの誘導テキスト必須** (例: tooltip や description で「詳細は星の軌跡を右クリック」)

### 9.5.3 右クリックリンク (JEI 起動)
- ターゲット:
  - **StarForge コアブロック** (controller) を空手で右クリック → 該当 JEI ページ (どれを開く? 候補: 全カテゴリ表示)
  - **star_locus アイテム** を右クリック → 該当 Trace の自作 JEI ページ
- 実装:
  - `IModPlugin.onRuntimeAvailable()` で `IJeiRuntime` を static field に保存
  - `Item.use()` / `Block.useWithoutItem()` から `runtime.getRecipesGui().showTypes(List.of(RECIPE_TYPE))` 呼び出し
  - 既存 credit `CraftPatternJeiPlugin` + `JeiNavigation` を流儀の参考に (編集不可、コピー流儀は OK)

---

## 10. 解決済み / 未確定事項

### 解決済み (Q1〜Q8)

- ✅ (Q1) 要求アイテムと所定アイテムは**別物**
- ✅ (Q2) 「コアブロック内部で決められた値」 = レシピ稼働ごとのランダム値 (0〜92%)、+8% で超消費開始
- ✅ (Q3) ver.0.1 の 80tick は誤記、正しくは 160tick
- ✅ (Q4) EnergyHatch = WEN 専用、超消費時は電線吸い尽くし + WEN 容量分
- ✅ (Q5) ソーラー = GTCEu Solar Panel
- ✅ (Q6) 太陽 成熟→崩壊 移行 = レシピ duration 経過
- ✅ (Q7) 通常型「履行」 = 所定アイテムの累計消費量が要求値に到達
- ✅ (Q8) スロット仕様 = items 1/15, fluids 0/12、JEI 二層 (自作 + GT 標準ダミー)、右クリックリンク (controller / star_locus)

### 未解決

- [ ] 各軌跡レシピの具体パラメータ:
  - duration (太陽/ブラックホール 成熟フェイズの加工時間)
  - 要求アイテム (構築フェイズで搬入する 1 回投入のアイテム、軌跡ごとに違う?)
  - 所定アイテム + 累計消費量 + tick あたり消費レート (通常型の崩壊フェイズ)
  - EUt (通常時 / 太陽生成量 / BH 成熟フェイズ放出量)
- [ ] 出力アイテム (各軌跡)
- [ ] 太陽: ソーラーの数量と tier (LV〜UV のどれを何個?)
- [ ] BH 成熟フェイズの瞬間放出エネルギー量
- [ ] BH 崩壊フェイズで搬出される通常アイテム (singularity 獲得失敗時の出力)

---

## 改訂履歴

- **ver.0.1**: 基本骨格、3 フェイズ、シンギュラリティ獲得、tier 解放
- **ver.0.2** (2026-05-10): 通常型/成熟型分類、太陽ダイソンキューブ、ブラックホール崩壊度、専用 EnergyHatch 概念
- **ver.0.3** (2026-05-10): Q1〜Q7 解決を反映 — フェイズ流れ明確化、要求/所定アイテム別、内部閾値ランダム値、超消費 160tick、EnergyHatch 詳細、ver.0.1 80tick→160tick 訂正
- **ver.0.4** (2026-05-10): スロット仕様確定 (items 1/15, fluids 0/12)、JEI 二層構造 (自作カテゴリ + GT 標準ダミー)、右クリックリンク (controller / star_locus)、§9.5 追加
